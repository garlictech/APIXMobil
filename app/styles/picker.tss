"Window": {
    height: 220,
    // inverse of the height allows us to have the picker slide in from
    // the bottom.
    bottom: -220,
    backgroundColor: "white"
}

"Label": {
    top: "0",
    backgroundColor: "white",
    color: "black"
}

"#cancel": {
    text_id: 'cancel'
}

"#done": {
    text_id: 'ok',
}

"#picker": {
    selectionIndicator: "true",
    useSpinner: "true",
    top: "20",
    height: "200"
}

"ToolBar": {
    top: 0
}
