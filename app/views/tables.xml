<Alloy>
    <Window id="window">
        <!-- Back button added programmatically -->
        <Label id="top_label" class="top bar"/>
        <!-- path view added programmatically -->
        <TableView id="table" class="table">
            <!-- Rows added programmatically -->
        </TableView>
        <Label class="bottom bar" />
        <Require src="info_button"/>
        <Require src="settings_button"/>
        <Require src="home_button"/>
        <!-- Ontime (set bookmark) button added programmatically -->
    </Window>
</Alloy>
